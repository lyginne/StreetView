using StreetView.OpenGL.Elements;

namespace StreetView.OpenGL.StreetElements
{
    public class Building
    {
        public Building(double x, double z, int stages, double length, double depth, Texture texture) {
            //Prism prism = new Prism(x, z, TODO, TODO, texture);
        }
    }
}
